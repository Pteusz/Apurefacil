{
  "instance": {
    "project": "Apurefacil",
    "invariant": "A conta armada é a relação fundamental. apurar() produz grupos[] onde cada grupo tem classificacao (algoritmo), intencao (override do usuário) e estado_efetivo (resolução). Todo recorte é uma view filtrada por estado_efetivo. Toda ação do usuário é set_intencao(grupo_id, flag) sobre a conta armada. Não existe estado paralelo, lista auxiliar ou matching fora da apuração.",
    "stack": ["Python", "FastAPI", "Pydantic", "RapidFuzz", "Vanilla JS"],
    "domain": "apuração de renda bancária",
    "decay_hours": 24
  },
  "phases": {
    "grupos_apuracao": {
      "status": "done",
      "next": "sessao_limpeza",
      "updated_at": "2026-05-08T03:42:00Z",
      "translation": "concluída"
    },
    "sessao_limpeza": {
      "status": "done",
      "next": "front_unificacao",
      "updated_at": "2026-05-08T04:20:00Z",
      "translation": "concluída"
    },
    "front_unificacao": {
      "status": "locked",
      "next": null,
      "updated_at": "2026-05-08T04:30:00Z",
      "translation": "Instrução: Unificar o front para ler grupos[] de currentApuracao diretamente. Modificar três arquivos: front/core.js, front/apuracao.js e front/pdf-tabela.js.\n\n── ARQUIVO 1: front/core.js ──────────────────────────────────────────────\n\n1. Remover a linha que declara knownFontes:\n   let knownFontes      = [];    // fontes acumuladas — persiste mesmo quando inativas na apuração\n   (remover linha inteira, incluindo o comentário)\n\n── ARQUIVO 2: front/apuracao.js ──────────────────────────────────────────\n\n2. Remover a função _mergeFontes() inteira (de 'function _mergeFontes' até o fechamento '}').\n\n3. Remover a função _grupoIsActive() inteira (de 'function _grupoIsActive' até o fechamento '}').\n\n4. Remover a função _sourceMatches() inteira (de 'function _sourceMatches' até o fechamento '}').\n\n5. Reescrever a função renderSourceCards() inteira. Substituir o corpo completo atual pelo seguinte:\n\nfunction renderSourceCards() {\n  const wrap = document.getElementById('source-cards');\n  if (!currentSession) { wrap.innerHTML = ''; return; }\n\n  const grupos = currentApuracao?.grupos || [];\n  if (grupos.length === 0) { wrap.innerHTML = ''; return; }\n\n  const renda = currentApuracao?.renda_apurada_mensal || 0;\n  const meses = currentApuracao?.meses_analisados || 0;\n\n  const fontesAtivas = grupos.filter(g => g.estado_efetivo === 'estavel');\n  const sysExcluidos = grupos.filter(g => g.estado_efetivo !== 'estavel');\n\n  const htmlAtivas = fontesAtivas.map((g) => {\n    const isSelected = activeSource === g.grupo_id;\n    const partPct    = renda > 0 ? (g.renda_base / renda * 100) : 0;\n    const barW       = Math.min(Math.max(partPct, 0), 100).toFixed(1);\n    const cv         = g.cv != null ? Math.round(g.cv * 100) : null;\n    const cvClass    = cv === null ? 'cv-badge--unknown' : cv < 50 ? 'cv-badge--low' : cv <= 80 ? 'cv-badge--mid' : 'cv-badge--high';\n    const barClass   = cv === null ? '' : cv < 50 ? 'source-card-bar--low' : cv <= 80 ? 'source-card-bar--mid' : 'source-card-bar--high';\n    const reg        = meses > 0 ? `${g.aparicoes}/${meses}` : '';\n    return `\n      <div class=\"source-card${isSelected ? ' active' : ''}\" data-grupo-id=\"${esc(g.grupo_id)}\">\n        <div class=\"source-card-header\">\n          <span class=\"source-card-name\" title=\"${esc(g.pagador)}\">${esc(g.pagador)}</span>\n          <button class=\"source-card-toggle\"\n            data-action=\"toggle-grupo\"\n            data-grupo-id=\"${esc(g.grupo_id)}\"\n            data-active=\"0\"\n            title=\"Desativar grupo\"\n          >Desativar</button>\n        </div>\n        <span class=\"source-card-val\">${fmtBRL(g.renda_base)}</span>\n        <div class=\"source-card-meta\">\n          <span class=\"source-card-regularidade\">${reg}</span>\n          <span class=\"cv-badge ${cvClass}\" title=\"Variabilidade\">${cv !== null ? cv + '%' : '?'}</span>\n        </div>\n        <div class=\"source-card-bar-wrap\">\n          <div class=\"source-card-bar ${barClass}\" style=\"width:${barW}%\"></div>\n        </div>\n      </div>`;\n  }).join('');\n\n  const totalPorTipo = {};\n  const countPorTipo = {};\n  sysExcluidos.forEach(g => {\n    const tipo = _MOTIVO_TO_TIPO[g.estado_efetivo] || g.estado_efetivo || 'outro';\n    const hist = g.valores_por_mes || {};\n    totalPorTipo[tipo] = (totalPorTipo[tipo] || 0) + Object.values(hist).reduce((a, v) => a + v, 0);\n    countPorTipo[tipo] = (countPorTipo[tipo] || 0) + 1;\n  });\n\n  const tiposOrdem   = ['circular','variancia','sem_historico','auto_transferencia','flag_usuario','duplicado_manual'];\n  const tiposPresentes = tiposOrdem.filter(t => totalPorTipo[t] || countPorTipo[t]);\n  const tiposExtras    = [...new Set(Object.keys(totalPorTipo).concat(Object.keys(countPorTipo)))].filter(t => !tiposOrdem.includes(t));\n\n  const renderTipoCard = (tipo) => {\n    const isSelected = activeTypeFilter === tipo;\n    const label      = TIPO_LABEL[tipo] || tipo;\n    const total      = totalPorTipo[tipo] || 0;\n    const count      = countPorTipo[tipo] || 0;\n    return `\n      <div class=\"source-card source-card-excl${isSelected ? ' active' : ''}\" data-tipo=\"${esc(tipo)}\" data-action=\"select-tipo\">\n        <div class=\"source-card-header\">\n          <span class=\"source-card-name\">${esc(label)}</span>\n          <span class=\"excl-count-badge\">${count} grupo${count !== 1 ? 's' : ''}</span>\n        </div>\n        <span class=\"source-card-val\">${fmtBRL(total)}</span>\n      </div>`;\n  };\n\n  const htmlExcl = [...tiposPresentes, ...tiposExtras].map(renderTipoCard).join('');\n  const separador = sysExcluidos.length > 0\n    ? `<div class=\"excl-section-label\">Excluídos pelo sistema</div>${htmlExcl}`\n    : '';\n\n  wrap.innerHTML = htmlAtivas + separador;\n\n  wrap.querySelectorAll('.source-card:not([data-action=\"select-tipo\"])').forEach(card => {\n    card.addEventListener('click', e => {\n      if (e.target.closest('[data-action=\"toggle-grupo\"]')) return;\n      const gid = card.dataset.grupoId;\n      if (!gid) return;\n      activeSource     = (activeSource === gid) ? null : gid;\n      activeTypeFilter = null;\n      renderSourceCards();\n      renderFilterBar();\n      renderTransactions();\n    });\n  });\n\n  wrap.querySelectorAll('[data-action=\"select-tipo\"]').forEach(card => {\n    card.addEventListener('click', () => {\n      const tipo       = card.dataset.tipo;\n      activeTypeFilter = (activeTypeFilter === tipo) ? null : tipo;\n      activeSource     = null;\n      renderSourceCards();\n      renderFilterBar();\n      renderTransactions();\n    });\n  });\n\n  wrap.querySelectorAll('[data-action=\"toggle-grupo\"]').forEach(btn => {\n    btn.addEventListener('click', e => {\n      e.stopPropagation();\n      const grupo_id = btn.dataset.grupoId;\n      if (!grupo_id) return;\n      sendMutation('toggle_grupo', null, { grupo_id, active: false });\n    });\n  });\n\n  requestAnimationFrame(() => {\n    const activeCards = [...wrap.querySelectorAll('.source-card:not([data-action=\"select-tipo\"])')]\n    if (activeCards.length === 0) return;\n    wrap.parentNode.querySelector('.source-ver-mais-btn')?.remove();\n    const firstTop = activeCards[0].getBoundingClientRect().top;\n    const hasMultipleRows = activeCards.some(c => c.getBoundingClientRect().top > firstTop + 4);\n    if (!hasMultipleRows) { wrap.style.maxHeight = ''; wrap.style.overflow = ''; return; }\n    if (!sourcesExpanded) {\n      const firstRowCards = activeCards.filter(c => c.getBoundingClientRect().top <= firstTop + 4);\n      const last    = firstRowCards[firstRowCards.length - 1];\n      const wrapTop = wrap.getBoundingClientRect().top;\n      wrap.style.maxHeight = (last.getBoundingClientRect().bottom - wrapTop) + 'px';\n      wrap.style.overflow  = 'hidden';\n    } else {\n      wrap.style.maxHeight = '';\n      wrap.style.overflow  = '';\n    }\n    const verMaisBtn = document.createElement('button');\n    verMaisBtn.className   = 'source-ver-mais-btn';\n    verMaisBtn.textContent = sourcesExpanded ? 'ver menos' : 'ver mais';\n    verMaisBtn.addEventListener('click', () => { sourcesExpanded = !sourcesExpanded; renderSourceCards(); });\n    wrap.parentNode.appendChild(verMaisBtn);\n  });\n}\n\n6. Na função renderFilterBar(), localizar o trecho que exibe o crumb de activeSource. O crumb usa 'activeSource' como texto de exibição. Substituir apenas o conteúdo do texto exibido: em vez de usar activeSource diretamente como label, buscar o pagador correspondente:\n   const sourceLabel = (currentApuracao?.grupos || []).find(g => g.grupo_id === activeSource)?.pagador || activeSource;\n   E usar sourceLabel no lugar de activeSource nos atributos title e no texto do span filter-crumb-text.\n\n7. Na função selectSession() (em apuracao.js), localizar a linha:\n   knownFontes      = [];\n   e removê-la.\n\n── ARQUIVO 3: front/pdf-tabela.js ────────────────────────────────────────\n\n8. Na função renderTransactions(), localizar o bloco do filtro activeSource:\n   if (activeSource) {\n     const fonteAtiva = knownFontes.find(f => f.pagador === activeSource);\n     if (fonteAtiva?.grupo_id) {\n       lancs = lancs.filter(l => l.state.grupo_id === fonteAtiva.grupo_id);\n     } else {\n       lancs = lancs.filter(l => _sourceMatches(l.state, activeSource));\n     }\n   }\n   Substituir por:\n   if (activeSource) {\n     lancs = lancs.filter(l => l.state.grupo_id === activeSource);\n   }\n\n9. Na função renderTransactions(), localizar o bloco do filtro activeTypeFilter:\n   if (activeTypeFilter) {\n     const grupoIdsDoTipo = new Set(\n       knownFontes\n         .filter(f => f.system_excluded &&\n           (_MOTIVO_TO_TIPO[f.motivo_exclusao] || f.motivo_exclusao) === activeTypeFilter)\n         .map(f => f.grupo_id)\n         .filter(Boolean)\n     );\n     lancs = grupoIdsDoTipo.size > 0\n       ? lancs.filter(l => grupoIdsDoTipo.has(l.state.grupo_id))\n       : [];\n   }\n   Substituir por:\n   if (activeTypeFilter) {\n     const grupoIdsDoTipo = new Set(\n       (currentApuracao?.grupos || [])\n         .filter(g => g.estado_efetivo !== 'estavel' &&\n           (_MOTIVO_TO_TIPO[g.estado_efetivo] || g.estado_efetivo) === activeTypeFilter)\n         .map(g => g.grupo_id)\n     );\n     lancs = grupoIdsDoTipo.size > 0\n       ? lancs.filter(l => grupoIdsDoTipo.has(l.state.grupo_id))\n       : [];\n   }\n\nVerificação: executar os três greps a seguir a partir de /workspace/Apurefacil e confirmar que NENHUM retorna resultado:\n   grep -rn 'knownFontes' front/\n   grep -rn '_mergeFontes\\|_grupoIsActive\\|_sourceMatches' front/\n   grep -rn 'system_excluded\\|motivo_exclusao' front/apuracao.js front/pdf-tabela.js\nSe algum grep retornar linhas, a fase falhou."
    }
  }
}
