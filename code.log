---

✓ **front_unificacao** concluída

**Executado:**
- `front/core.js`: removida declaração `let knownFontes = []`
- `front/apuracao.js`: removidas `_mergeFontes()`, `_grupoIsActive()`, `_sourceMatches()`; `renderSourceCards()` reescrita para ler `currentApuracao.grupos[]` diretamente; `renderFilterBar()` atualizada para buscar `pagador` por `grupo_id`; limpos todos os `knownFontes = []` (selectSession, handleSessionMenuAction)
- `front/pdf-tabela.js`: filtros de `activeSource` e `activeTypeFilter` simplificados para usar `grupos[]`
- `front/app.js`: removidos dois resíduos de `knownFontes = []` nos reset-blocks (não cobertos pela translation, mas necessários para a verificação passar)

**Verificação:** passou — todos os três greps retornaram vazio

**Próxima fase:** `null` — sistema completo
